# Database - CONFIGURACIÓN REMOTA EN LA NUBE
# Solicitar las credenciales al equipo de desarrollo
# Formato: postgresql://usuario:contraseña@host:puerto/nombre_base_datos
# IMPORTANTE: NO usar comillas en los valores
DATABASE_URL=postgresql://username:password@tu-servidor-postgresql.com:5432/rendiciones_db

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Server
PORT=5100
NODE_ENV=development

# URLs base para diferentes entornos
# En desarrollo: se detecta automáticamente
# En producción: configurar la URL base del servidor
BASE_URL=https://api.parsedemo.axiomacloud.com

# CORS - URL del Frontend (IMPORTANTE: Debe coincidir con la URL que usan los usuarios)
# ⚠️ CRÍTICO: Los emails de verificación usan esta URL. Si los usuarios acceden por dominio,
#    configura el dominio aquí. Si acceden por IP, usa la IP. ¡Deben coincidir!
# Desarrollo:
# FRONTEND_URL=http://localhost:3000
# Producción con dominio (RECOMENDADO):
FRONTEND_URL=https://parsedemo.axiomacloud.com
# Producción con IP (solo si no tienes dominio):
# FRONTEND_URL=http://192.168.1.100:8087

# File uploads
MAX_FILE_SIZE=10mb
UPLOAD_DIR=uploads

# ===== INTEGRACIÓN CON MODELOS DE IA =====
# Habilitar extracción con IA (true/false)
ENABLE_AI_EXTRACTION=false

# OPCIÓN 1: OpenAI GPT (Pago por uso, muy preciso)
# Costo aproximado: $0.50-$2.00 por cada 1000 documentos procesados
# Registro: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-xxx

# OPCIÓN 2: Anthropic Claude (Pago por uso, excelente para documentos)
# Costo aproximado: $0.30-$1.50 por cada 1000 documentos procesados  
# Registro: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxx

# OPCIÓN 3: Google Gemini (Gratis hasta 15 req/min, luego pago)
# Gratis: 1500 requests por día
# Registro: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=xxx

# OPCIÓN 4: Ollama (Completamente gratis, ejecuta localmente)
# Descarga: https://ollama.ai/
# Instalación: ollama run llama3.2
OLLAMA_ENABLED=false
OLLAMA_HOST=http://localhost:11434

# ===== AUTENTICACIÓN CON GOOGLE OAUTH =====
# Registro: https://console.cloud.google.com/apis/credentials
# 1. Crear nuevo proyecto
# 2. Habilitar Google+ API
# 3. Crear credenciales OAuth 2.0
# 4. Agregar URL autorizada: https://parsedemo.axiomacloud.com
# 5. Agregar redirect URI: https://api.parsedemo.axiomacloud.com/api/auth/google/callback

# IMPORTANTE: Si no usas login con Google, puedes poner valores dummy
GOOGLE_CLIENT_ID=dummy-client-id
GOOGLE_CLIENT_SECRET=dummy-client-secret
GOOGLE_CALLBACK_URL=https://api.parsedemo.axiomacloud.com/api/auth/google/callback